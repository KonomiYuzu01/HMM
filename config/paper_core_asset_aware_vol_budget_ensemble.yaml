base_config: config/paper_core_asset_aware_vol_budget.yaml
random_seeds: [7, 42, 123]

member_overrides:
  max_gross_leverage: 1.0
  risk_off_growth_floor:
    enabled: true
    account_share: 0.20
    disable_when_volatility_stressed: true
    core_weights:
      QQQ: 0.50
      SEMIS: 0.50

ensemble:
  rebalance_every_days: 21
  calendar_anchored_schedule: true
  no_trade_turnover: 0.01
  cost_bps_per_dollar_traded: 7.5

output_dir: output/paper_core_asset_aware_vol_budget_ensemble
